# System Architecture

Frontend (React)
        |
        v
Backend (Node.js + Express)
        |
        v
Database (MongoDB)

Data Flow:
User → Frontend → Backend API → MongoDB
MongoDB → Backend API → Frontend → User
## Components
- Frontend
- backend
- Database

